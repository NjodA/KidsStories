   <div class=down>
   <br>
    <div class="flex1">


<h2> مرحباً   <%= @user.name %></h2>

<% @user.stickers.each do |s| %>
 <img src=<%=s.image %> >
<% end %>
</div>
</div>
<% time=0 %>
<% @user.readings.each do |r| %>
    <% time+= ((r.updated_at - r.created_at) / 60).round(2) %>
<% end %>
<p><%= time %></p>